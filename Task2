time_in_second = int(input("Введите время в секундах >>> "))

if time_in_second
Hours = time_in_second // 3600

Minutes = (time_in_second - Hours * 3600) // 60

Seconds = int(time_in_second - Hours * 3600 - Minutes * 60)

print(f"{Hours}:{Minutes}:{Seconds}")
